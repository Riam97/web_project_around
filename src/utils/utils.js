/* import * as indexCode from "./constants.js";

indexCode.profileFormElement.addEventListener(
  "submit",
  indexCode.handleFormSubmit
);

indexCode.editProfileButton.addEventListener("click", function (event) {
  event.stopPropagation();
  indexCode.openProfilePopup();
});

indexCode.addCardButton.addEventListener("click", function (event) {
  event.stopPropagation();
  indexCode.openAddCardPopup();
});

indexCode.closeButton.forEach((button) => {
  button.addEventListener("click", indexCode.closePopup);
});

document.addEventListener("keydown", indexCode.closePopupEsc); */
